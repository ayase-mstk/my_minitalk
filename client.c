#include <stdlib.h>
#include <signal.h>
#include <stdio.h>

int main(int ac, char **av)
{
	while()
	{
		if()
		{
			kill((pid_t)atoi(av[1]), SIGUSR1);
		}
		else if ()
		{
			kill((pid_t)atoi(av[1]), SIGUSR2);
		}
		i++;
	}
	return (0);
}
